---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: autonomousactions.
spec:
  group: ""
  names:
    kind: AutonomousAction
    listKind: AutonomousActionList
    plural: autonomousactions
    singular: autonomousaction
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.actionCount
      name: Actions
      type: integer
    - jsonPath: .status.lastTriggered
      name: Last Triggered
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: ""
    schema:
      openAPIV3Schema:
        description: AutonomousAction is the Schema for the autonomousactions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AutonomousActionSpec defines the desired state of AutonomousAction
            properties:
              approvalMode:
                description: 'Approval mode: "autonomous", "human-in-loop", "dry-run"'
                type: string
              constraints:
                description: Action constraints
                properties:
                  allowedActions:
                    description: 'Allowed action types: "scale", "restart", "rollback",
                      "drain", "custom"'
                    items:
                      type: string
                    type: array
                  cooldownSeconds:
                    description: Cooldown period between actions
                    format: int32
                    type: integer
                  forbiddenNamespaces:
                    description: Forbidden namespaces
                    items:
                      type: string
                    type: array
                  maxConcurrent:
                    description: Max concurrent actions
                    type: integer
                type: object
              context:
                description: Context sources for LLM decision-making
                properties:
                  includeEvents:
                    description: Include recent events
                    type: boolean
                  includeHubble:
                    description: Include Hubble flows
                    type: boolean
                  includeK8sGPT:
                    description: Include K8sGPT analysis
                    type: boolean
                  includeMetrics:
                    description: Include Prometheus metrics
                    type: boolean
                  namespaces:
                    description: Namespace scope
                    items:
                      type: string
                    type: array
                  timeWindow:
                    description: Time window for context (e.g., "5m", "1h")
                    type: string
                type: object
              llm:
                description: LLM configuration
                properties:
                  apiKeySecret:
                    description: API key for cloud providers (stored in Secret)
                    type: string
                  endpoint:
                    description: Endpoint for local inference (Ollama/vLLM)
                    type: string
                  maxTokens:
                    description: Max tokens
                    type: integer
                  model:
                    description: Model name (e.g., "phi-3", "llama-3.2", "gpt-4")
                    type: string
                  provider:
                    description: 'Provider: "ollama", "vllm", "openai", "anthropic"'
                    type: string
                  systemPrompt:
                    description: System prompt template
                    type: string
                  temperature:
                    description: Temperature for generation (0-1)
                    type: number
                required:
                - model
                - provider
                type: object
              mcpServer:
                description: MCP server endpoint for external agent integration
                type: string
              trigger:
                description: Trigger conditions for autonomous action
                properties:
                  anomalyScoreThreshold:
                    description: Anomaly score threshold (0-1)
                    type: number
                  errorBudgetThreshold:
                    description: SLO error budget threshold
                    type: number
                  eventPattern:
                    description: Event pattern to match
                    type: string
                  forecastThreshold:
                    description: Forecast threshold (percentage change)
                    type: number
                  type:
                    description: 'Trigger type: "anomaly", "slo-violation", "forecast",
                      "event"'
                    type: string
                required:
                - type
                type: object
            required:
            - approvalMode
            - context
            - llm
            - trigger
            type: object
          status:
            description: AutonomousActionStatus defines the observed state of AutonomousAction
            properties:
              actionCount:
                description: Action count
                format: int32
                type: integer
              conditions:
                description: Conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              errorMessage:
                description: Error message
                type: string
              executionResult:
                description: Execution result
                properties:
                  durationSeconds:
                    description: Duration in seconds
                    type: number
                  executedAt:
                    description: Execution time
                    format: date-time
                    type: string
                  output:
                    description: Output/result message
                    type: string
                  success:
                    description: Success status
                    type: boolean
                type: object
              lastTriggered:
                description: Last trigger time
                format: date-time
                type: string
              phase:
                description: 'Phase: "Monitoring", "Triggered", "Reasoning", "Approved",
                  "Executing", "Completed", "Failed", "Rejected"'
                type: string
              proposedAction:
                description: Proposed action
                properties:
                  confidence:
                    description: Confidence score (0-1)
                    type: number
                  description:
                    description: Action description
                    type: string
                  parameters:
                    description: Action parameters (JSON)
                    type: string
                  riskLevel:
                    description: 'Risk level: "low", "medium", "high"'
                    type: string
                  type:
                    description: Action type
                    type: string
                required:
                - description
                - type
                type: object
              reasoning:
                description: LLM reasoning output
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
