---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: anomalyactions.aiops.prophet.io
spec:
  group: aiops.prophet.io
  names:
    kind: AnomalyAction
    listKind: AnomalyActionList
    plural: anomalyactions
    singular: anomalyaction
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.lastDetected
      name: Last Detected
      type: date
    - jsonPath: .status.remediationCount
      name: Remediations
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AnomalyAction is the Schema for the anomalyactions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AnomalyActionSpec defines the desired state of AnomalyAction
            properties:
              k8sgpt:
                description: Integration with K8sGPT for diagnostics
                properties:
                  enabled:
                    description: Enable automatic K8sGPT analysis on anomaly
                    type: boolean
                  endpoint:
                    description: K8sGPT service endpoint
                    type: string
                type: object
              metric:
                description: Metric name or query that triggered the anomaly
                type: string
              remediation:
                description: Remediation actions to take
                properties:
                  cooldownSeconds:
                    description: Cooldown period in seconds before allowing another
                      remediation
                    format: int32
                    type: integer
                  podSelector:
                    additionalProperties:
                      type: string
                    description: 'For restart action: pod selector'
                    type: object
                  replicas:
                    description: 'For scale action: target replica count'
                    format: int32
                    type: integer
                  requireApproval:
                    description: 'Require approval before executing (default: false)'
                    type: boolean
                  type:
                    description: 'Action type: "restart", "scale", "rollback", "alert"'
                    type: string
                required:
                - type
                type: object
              source:
                description: Anomaly detection source (e.g., "grafana-ml", "prometheus",
                  "otel")
                type: string
              target:
                description: Target resources (namespace, label selectors)
                properties:
                  labels:
                    additionalProperties:
                      type: string
                    description: Label selectors
                    type: object
                  namespace:
                    description: Namespace to target
                    type: string
                  resourceType:
                    description: 'Resource type: "Deployment", "StatefulSet", "Pod"'
                    type: string
                required:
                - namespace
                - resourceType
                type: object
              threshold:
                description: Threshold or condition that defines an anomaly
                type: string
              webhookUrl:
                description: Webhook URL for external notifications (e.g., PagerDuty)
                type: string
            required:
            - metric
            - remediation
            - source
            - target
            type: object
          status:
            description: AnomalyActionStatus defines the observed state of AnomalyAction
            properties:
              conditions:
                description: Conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              errorMessage:
                description: Error message if remediation failed
                type: string
              k8sgptAnalysis:
                description: K8sGPT analysis result
                type: string
              lastDetected:
                description: Last anomaly detection time
                format: date-time
                type: string
              lastRemediated:
                description: Last remediation time
                format: date-time
                type: string
              phase:
                description: 'Phase: "Pending", "Detected", "Remediating", "Resolved",
                  "Failed"'
                type: string
              remediationCount:
                description: Number of remediations performed
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
